# 编辑器界面重构方案 - 最终版

## 📋 方案概述

本方案旨在将 `EditorView` 重构为一个现代、高效、以视频为中心的编辑界面。核心理念是对标现代 NLE（非线性编辑）软件的布局逻辑，同时优化多任务转录场景的用户体验。

### 核心设计理念

1. **视频优先 (Video First)**：利用 CSS Grid 确保视频画面占据最大空间，波形图退居辅助地位
2. **极简顶栏 (Clean Header)**：去除手动保存按钮，采用"视觉中心"布局和"悬浮交互"逻辑
3. **全局任务掌控**：通过 TaskMonitor（类似 Edge 下载中心）实现多任务并行管理
4. **智能进度显示**：顶栏中心根据状态动态显示当前任务进度或全局队列进度
5. **沉浸式编辑**：优化波形图滚动条、状态栏等细节，提升整体视觉体验

---

## 🎯 整体布局架构

```
┌────────────────────────────────────────────────────────────────┐
│  Header (56px)                                                 │
│  ├─ 左：返回按钮 + 任务信息堆叠                                   │
│  ├─ 中：动态进度区（当前任务进度 / 队列总进度）                     │
│  └─ 右：任务监控 + 撤销/重做 + 导出                                │
├────────────────────────────────────────────────────────────────┤
│  Main (flex: 1)                                                │
│  ┌───────────────────────────┬─────────────────────────────┐  │
│  │  Stage Column (Grid)      │  Sidebar (350px, 可拖拽调整)  │  │
│  │  ┌───────────────────┐   │  ┌─────────────────────────┐ │  │
│  │  │                   │   │  │  Tab Nav                │ │  │
│  │  │  Video Stage      │   │  ├─────────────────────────┤ │  │
│  │  │  (1fr)            │   │  │                         │ │  │
│  │  │                   │   │  │  字幕列表                │ │  │
│  │  └───────────────────┘   │  │  (可滚动)               │ │  │
│  │  ┌───────────────────┐   │  │                         │ │  │
│  │  │  Playback Controls│   │  └─────────────────────────┘ │  │
│  │  │  (48px)           │   │                             │  │
│  │  └───────────────────┘   │                             │  │
│  │  ┌───────────────────┐   │                             │  │
│  │  │  Waveform Timeline│   │                             │  │
│  │  │  (160px)          │   │                             │  │
│  │  └───────────────────┘   │                             │  │
│  └───────────────────────────┴─────────────────────────────┘  │
├────────────────────────────────────────────────────────────────┤
│  Footer (28px)                                                 │
│  左：字幕总数 | 当前字幕   中：自动保存时间   右：问题数 | 设置   │
└────────────────────────────────────────────────────────────────┘
```

---

## 📂 文件结构调整

建议新建/拆分以下组件以保持代码整洁：

```
frontend/src/
├── views/
│   └── EditorView.vue                    // 主布局容器 (Grid)
├── components/editor/
│   ├── EditorHeader.vue                  // [新建] 独立顶栏组件
│   ├── TaskMonitor/
│   │   └── index.vue                     // [新建] 任务监控面板
│   ├── VideoStage/
│   │   └── index.vue                     // 视频播放区
│   ├── PlaybackControls/
│   │   └── index.vue                     // 播放控制条 (底座模式)
│   ├── WaveformTimeline/
│   │   └── index.vue                     // 波形图 (优化滚动条)
│   └── SubtitleList/
│       └── index.vue                     // 字幕列表 (350px优化)
```

---

## 🛠️ 详细实施方案

### 一、顶栏重构 (EditorHeader.vue)

#### 组件职责
- 导航控制（返回按钮）
- 任务信息展示（名称、状态）
- 动态进度显示（当前任务 / 队列总进度）
- 全局操作入口（任务监控、撤销/重做、导出）

#### 布局结构

```vue
<template>
  <header class="editor-header">
    <!-- 左侧：返回 + 任务信息堆叠 -->
    <div class="header-left">
      <el-button link class="nav-back" @click="$router.push('/tasks')">
        <el-icon :size="20"><ArrowLeft /></el-icon>
      </el-button>

      <div class="divider-vertical"></div>

      <div class="task-info-stack">
        <h1 class="task-name" :title="taskName">{{ taskName }}</h1>
        <div class="task-meta">
          <span class="status-dot" :class="statusClass"></span>
          <span class="meta-text">{{ metaText }}</span>
        </div>
      </div>
    </div>

    <!-- 中间：动态进度区 -->
    <div class="header-center">
      <!-- 场景1: 当前任务转录中 -->
      <el-popover
        v-if="isCurrentTaskTranscribing"
        trigger="hover"
        :width="220"
        popper-class="control-popover-dark"
        :show-after="200"
        placement="bottom"
      >
        <template #reference>
          <div class="progress-capsule">
            <div class="progress-track">
              <div class="progress-fill" :style="{ width: currentTaskProgress + '%' }"></div>
            </div>
            <span class="capsule-text">转录中 {{ currentTaskProgress }}%</span>
          </div>
        </template>

        <div class="hover-controls">
          <div class="label">当前任务控制</div>
          <div class="btn-group">
            <el-button circle size="small" @click="$emit('pause')">
              <el-icon><VideoPause /></el-icon>
            </el-button>
            <el-button circle size="small" type="danger" plain @click="$emit('cancel')">
              <el-icon><Close /></el-icon>
            </el-button>
          </div>
        </div>
      </el-popover>

      <!-- 场景2: 当前任务完成，显示队列总进度 -->
      <div v-else class="queue-progress">
        <div class="progress-track">
          <div class="progress-fill" :style="{ width: queueProgressPercent + '%' }"></div>
        </div>
        <span class="progress-text">
          {{ queueCompleted }}/{{ queueTotal }} 任务完成
          <span v-if="queueProgressPercent === 100" class="complete-check">✓</span>
        </span>
      </div>
    </div>

    <!-- 右侧：操作按钮 -->
    <div class="header-right">
      <!-- 任务监控器 -->
      <el-popover
        trigger="click"
        :width="400"
        popper-class="task-monitor-popover"
        placement="bottom-end"
      >
        <template #reference>
          <div class="monitor-trigger">
            <el-button text class="icon-btn">
              <el-icon :size="20"><Monitor /></el-icon>
            </el-button>
            <span v-if="activeTasks > 0" class="badge">{{ activeTasks }}</span>
          </div>
        </template>
        <TaskMonitor />
      </el-popover>

      <div class="divider-vertical"></div>

      <!-- 撤销/重做 -->
      <el-tooltip content="撤销">
        <el-button text class="icon-btn" :disabled="!canUndo" @click="$emit('undo')">
          <el-icon><RefreshLeft /></el-icon>
        </el-button>
      </el-tooltip>
      <el-tooltip content="重做">
        <el-button text class="icon-btn" :disabled="!canRedo" @click="$emit('redo')">
          <el-icon><RefreshRight /></el-icon>
        </el-button>
      </el-tooltip>

      <!-- 导出按钮 -->
      <el-button type="primary" size="small" class="export-btn" @click="$emit('export')">
        <el-icon><Download /></el-icon> 导出
      </el-button>
    </div>
  </header>
</template>
```

#### 动态进度逻辑

```javascript
const props = defineProps({
  taskName: String,
  currentTaskStatus: String,      // 'processing', 'finished', etc.
  currentTaskProgress: Number,    // 0-100
  queueCompleted: Number,         // 已完成任务数
  queueTotal: Number,             // 总任务数
  canUndo: Boolean,
  canRedo: Boolean,
  activeTasks: Number
})

const isCurrentTaskTranscribing = computed(() =>
  ['processing', 'queued'].includes(props.currentTaskStatus)
)

const queueProgressPercent = computed(() =>
  props.queueTotal > 0 ? (props.queueCompleted / props.queueTotal) * 100 : 0
)

const statusClass = computed(() => {
  if (isCurrentTaskTranscribing.value) return 'processing'
  if (queueProgressPercent.value === 100) return 'complete'
  return 'idle'
})

const metaText = computed(() => {
  if (isCurrentTaskTranscribing.value) {
    return `转录中 ${props.currentTaskProgress}%`
  }
  const now = new Date()
  const time = now.toLocaleTimeString('zh-CN', { hour: '2-digit', minute: '2-digit' })
  return `自动保存于 ${time}`
})
```

#### 样式要点

```scss
$header-h: 56px;
$bg: #1e1e1e;
$text: #eee;
$text-sub: #888;

.editor-header {
  height: $header-h;
  background: $bg;
  border-bottom: 1px solid #333;
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 0 16px;
  position: relative;
}

// 左侧堆叠布局
.header-left {
  display: flex;
  align-items: center;
  gap: 12px;

  .task-info-stack {
    display: flex;
    flex-direction: column;
    gap: 2px;

    .task-name {
      margin: 0;
      font-size: 14px;
      font-weight: 500;
      color: $text;
      max-width: 300px;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
    }

    .task-meta {
      display: flex;
      align-items: center;
      gap: 5px;

      .status-dot {
        width: 6px;
        height: 6px;
        border-radius: 50%;

        &.processing { background: #58a6ff; }
        &.complete { background: #3fb950; }
        &.idle { background: #8b949e; }
      }

      .meta-text {
        font-size: 11px;
        color: $text-sub;
      }
    }
  }
}

// 中间绝对定位居中
.header-center {
  position: absolute;
  left: 50%;
  top: 50%;
  transform: translate(-50%, -50%);
}

// 当前任务进度胶囊
.progress-capsule {
  background: #2d333b;
  border-radius: 12px;
  padding: 4px 14px;
  display: flex;
  align-items: center;
  gap: 10px;
  cursor: pointer;
  transition: all 0.2s;

  &:hover {
    background: #373e47;
    transform: scale(1.02);
  }

  .progress-track {
    width: 120px;
    height: 4px;
    background: #444c56;
    border-radius: 2px;
    overflow: hidden;

    .progress-fill {
      height: 100%;
      background: #58a6ff;
      transition: width 0.3s ease;
    }
  }

  .capsule-text {
    font-size: 12px;
    color: #adbac7;
    white-space: nowrap;
  }
}

// 队列总进度
.queue-progress {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 6px 16px;
  background: #2d333b;
  border-radius: 10px;

  .progress-track {
    width: 150px;
    height: 4px;
    background: #444c56;
    border-radius: 2px;
    overflow: hidden;

    .progress-fill {
      height: 100%;
      background: #3fb950;
      transition: width 0.5s ease;
    }
  }

  .progress-text {
    font-size: 12px;
    color: #adbac7;
    white-space: nowrap;
    display: flex;
    align-items: center;
    gap: 6px;

    .complete-check {
      color: #3fb950;
      font-weight: bold;
    }
  }
}

// 右侧按钮组
.header-right {
  display: flex;
  align-items: center;
  gap: 8px;

  .monitor-trigger {
    position: relative;
    display: flex;
    align-items: center;

    .badge {
      position: absolute;
      top: 4px;
      right: 4px;
      background: #58a6ff;
      color: #fff;
      font-size: 10px;
      padding: 0 4px;
      min-width: 16px;
      height: 16px;
      border-radius: 8px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-weight: 600;
    }
  }

  .icon-btn {
    width: 36px;
    height: 36px;
    padding: 0;
    color: #adbac7;

    &:hover:not([disabled]) {
      color: #fff;
      background: #373e47;
    }

    &[disabled] {
      opacity: 0.4;
      cursor: not-allowed;
    }
  }

  .export-btn {
    height: 32px;
    padding: 0 16px;
  }
}

.divider-vertical {
  width: 1px;
  height: 20px;
  background: #444c56;
  margin: 0 4px;
}
```

---

### 二、主布局改造 (EditorView.vue)

#### 核心改动

**模板结构**

```vue
<template>
  <div class="editor-view">
    <!-- 顶部导航栏 -->
    <EditorHeader
      :task-name="projectName"
      :current-task-status="taskStatus"
      :current-task-progress="taskProgress"
      :queue-completed="queueCompleted"
      :queue-total="queueTotal"
      :can-undo="canUndo"
      :can-redo="canRedo"
      :active-tasks="activeTasks"
      @undo="undo"
      @redo="redo"
      @export="showExportDialog"
      @pause="pauseTranscription"
      @cancel="cancelTranscription"
    />

    <!-- 加载/错误状态 (保持不变) -->
    <div v-if="isLoading" class="loading-overlay">...</div>
    <div v-else-if="loadError" class="error-overlay">...</div>

    <!-- 主编辑区域 -->
    <main v-else class="workspace-grid">
      <!-- 左侧舞台列 -->
      <section class="stage-column">
        <div class="video-wrapper">
          <VideoStage
            :job-id="jobId"
            :show-subtitle="true"
            @loaded="handleVideoLoaded"
            @error="handleVideoError"
          />
        </div>

        <div class="controls-wrapper">
          <PlaybackControls :pedestal="true" />
        </div>

        <div class="waveform-wrapper">
          <WaveformTimeline
            :job-id="jobId"
            @ready="handleWaveformReady"
            @region-update="handleRegionUpdate"
            @region-click="handleRegionClick"
          />
        </div>
      </section>

      <!-- 可拖拽分隔条 -->
      <div class="resizer" @mousedown="startResize"></div>

      <!-- 右侧边栏 -->
      <aside class="sidebar-column">
        <div class="tab-nav">
          <button class="tab-btn" :class="{ active: activeTab === 'subtitles' }" @click="activeTab = 'subtitles'">
            字幕列表
          </button>
          <button class="tab-btn" :class="{ active: activeTab === 'validation' }" @click="activeTab = 'validation'">
            问题检查
            <span v-if="errorCount > 0" class="badge">{{ errorCount }}</span>
          </button>
          <button class="tab-btn" :class="{ active: activeTab === 'assistant' }" @click="activeTab = 'assistant'">
            AI 助手
          </button>
        </div>

        <div class="tab-content">
          <div v-show="activeTab === 'subtitles'" class="tab-pane">
            <SubtitleList />
          </div>
          <div v-show="activeTab === 'validation'" class="tab-pane">
            <!-- 问题检查内容 -->
          </div>
          <div v-show="activeTab === 'assistant'" class="tab-pane">
            <!-- AI 助手内容 -->
          </div>
        </div>
      </aside>
    </main>

    <!-- 底部状态栏 -->
    <footer class="editor-footer">
      <div class="footer-left">
        <span>{{ totalSubtitles }} 条字幕</span>
        <span v-if="currentSubtitle" class="divider">|</span>
        <span v-if="currentSubtitle">当前: #{{ currentSubtitleIndex + 1 }}</span>
      </div>

      <div class="footer-center">
        <span v-if="lastSaved" class="save-time">
          <svg class="icon" viewBox="0 0 24 24" fill="currentColor">
            <path d="M9 16.2L4.8 12l-1.4 1.4L9 19 21 7l-1.4-1.4L9 16.2z"/>
          </svg>
          自动保存于 {{ formatLastSaved(lastSaved) }}
        </span>
      </div>

      <div class="footer-right">
        <span v-if="errorCount > 0" class="error-indicator" @click="activeTab = 'validation'">
          {{ errorCount }} 个问题
        </span>
        <span class="divider">|</span>
        <button class="settings-btn" @click="showSettings" title="设置">
          <svg viewBox="0 0 24 24" fill="currentColor">
            <path d="M19.14,12.94c0.04-0.3,0.06-0.61,0.06-0.94c0-0.32-0.02-0.64-0.07-0.94l2.03-1.58c0.18-0.14,0.23-0.41,0.12-0.61 l-1.92-3.32c-0.12-0.22-0.37-0.29-0.59-0.22l-2.39,0.96c-0.5-0.38-1.03-0.7-1.62-0.94L14.4,2.81c-0.04-0.24-0.24-0.41-0.48-0.41 h-3.84c-0.24,0-0.43,0.17-0.47,0.41L9.25,5.35C8.66,5.59,8.12,5.92,7.63,6.29L5.24,5.33c-0.22-0.08-0.47,0-0.59,0.22L2.74,8.87 C2.62,9.08,2.66,9.34,2.86,9.48l2.03,1.58C4.84,11.36,4.8,11.69,4.8,12s0.02,0.64,0.07,0.94l-2.03,1.58 c-0.18,0.14-0.23,0.41-0.12,0.61l1.92,3.32c0.12,0.22,0.37,0.29,0.59,0.22l2.39-0.96c0.5,0.38,1.03,0.7,1.62,0.94l0.36,2.54 c0.05,0.24,0.24,0.41,0.48,0.41h3.84c0.24,0,0.44-0.17,0.47-0.41l0.36-2.54c0.59-0.24,1.13-0.56,1.62-0.94l2.39,0.96 c0.22,0.08,0.47,0,0.59-0.22l1.92-3.32c0.12-0.22,0.07-0.47-0.12-0.61L19.14,12.94z M12,15.6c-1.98,0-3.6-1.62-3.6-3.6 s1.62-3.6,3.6-3.6s3.6,1.62,3.6,3.6S13.98,15.6,12,15.6z"/>
          </svg>
        </button>
      </div>
    </footer>
  </div>
</template>
```

**样式关键点**

```scss
.editor-view {
  display: flex;
  flex-direction: column;
  height: 100vh;
  background: var(--bg-base);
  color: var(--text-normal);
  overflow: hidden;
}

// 主工作区 Grid 布局
.workspace-grid {
  flex: 1;
  display: grid;
  grid-template-columns: 1fr 4px 350px; // 左侧 | 分隔条 | 右侧
  height: 100%;
  overflow: hidden;
}

// 舞台列 (三明治结构)
.stage-column {
  display: grid;
  grid-template-rows: 1fr 48px 160px; // 视频 | 控制 | 波形
  background: #000;
  min-width: 0;
  overflow: hidden;

  .video-wrapper {
    display: flex;
    justify-content: center;
    align-items: center;
    overflow: hidden;
  }

  .controls-wrapper {
    background: #1e1e1e;
    border-top: 1px solid #2a2a2a;
  }

  .waveform-wrapper {
    background: #1a1a1a;
    border-top: 1px solid #333;
    overflow: hidden;
  }
}

// 可拖拽分隔条
.resizer {
  width: 4px;
  background: var(--border-default);
  cursor: col-resize;
  transition: background 0.2s;
  position: relative;
  z-index: 10;

  &:hover {
    background: var(--primary);
  }

  &:active {
    background: var(--primary);
  }
}

// 侧边栏
.sidebar-column {
  display: flex;
  flex-direction: column;
  background: var(--bg-primary);
  border-left: 1px solid var(--border-default);
  min-width: 280px;
  max-width: 600px;
  overflow: hidden;
}

// 底部状态栏
.editor-footer {
  display: flex;
  align-items: center;
  justify-content: space-between;
  height: 28px;
  padding: 0 16px;
  background: var(--bg-secondary);
  border-top: 1px solid var(--border-default);
  font-size: 12px;
  color: var(--text-muted);
  flex-shrink: 0;

  .footer-left, .footer-center, .footer-right {
    display: flex;
    align-items: center;
    gap: 8px;
  }

  .save-time {
    display: flex;
    align-items: center;
    gap: 4px;
    color: var(--text-secondary);

    .icon {
      width: 14px;
      height: 14px;
      color: var(--success);
    }
  }

  .error-indicator {
    color: var(--danger);
    cursor: pointer;

    &:hover {
      text-decoration: underline;
    }
  }

  .settings-btn {
    width: 24px;
    height: 24px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--text-muted);
    border-radius: 4px;
    transition: all 0.2s;

    svg {
      width: 16px;
      height: 16px;
    }

    &:hover {
      background: var(--bg-tertiary);
      color: var(--text-normal);
    }
  }

  .divider {
    color: var(--border-default);
  }
}
```

**JavaScript 逻辑补充**

```javascript
import { ref, computed, onMounted, onUnmounted } from 'vue'
import { useUnifiedTaskStore } from '@/stores/unifiedTaskStore'
import EditorHeader from '@/components/editor/EditorHeader.vue'

const taskStore = useUnifiedTaskStore()

// 队列进度计算
const queueCompleted = computed(() => {
  return taskStore.tasks.filter(t => t.status === 'finished').length
})

const queueTotal = computed(() => {
  return taskStore.tasks.filter(t => t.status !== 'created').length
})

const activeTasks = computed(() => {
  return taskStore.tasks.filter(t =>
    ['processing', 'queued'].includes(t.status)
  ).length
})

// 拖拽调整分隔条
const isResizing = ref(false)
let sidebarWidth = ref(350)

function startResize(e) {
  isResizing.value = true
  document.addEventListener('mousemove', onResize)
  document.addEventListener('mouseup', stopResize)
}

function onResize(e) {
  if (!isResizing.value) return
  const newWidth = window.innerWidth - e.clientX
  sidebarWidth.value = Math.max(280, Math.min(600, newWidth))

  // 动态更新 Grid 列宽
  const grid = document.querySelector('.workspace-grid')
  if (grid) {
    grid.style.gridTemplateColumns = `1fr 4px ${sidebarWidth.value}px`
  }
}

function stopResize() {
  isResizing.value = false
  document.removeEventListener('mousemove', onResize)
  document.removeEventListener('mouseup', stopResize)

  // 保存用户偏好
  localStorage.setItem('editor-sidebar-width', sidebarWidth.value)
}

onMounted(() => {
  // 恢复用户偏好
  const saved = localStorage.getItem('editor-sidebar-width')
  if (saved) {
    sidebarWidth.value = parseInt(saved)
    const grid = document.querySelector('.workspace-grid')
    if (grid) {
      grid.style.gridTemplateColumns = `1fr 4px ${sidebarWidth.value}px`
    }
  }
})
```

---

### 三、播放控制优化 (PlaybackControls/index.vue)

#### 新增 "底座模式" Props

```javascript
const props = defineProps({
  pedestal: { type: Boolean, default: false }, // 新增
  compact: { type: Boolean, default: false },
  showSpeed: { type: Boolean, default: true },
  showVolume: { type: Boolean, default: true },
  showLoop: { type: Boolean, default: true },
  seekStep: { type: Number, default: 5 }
})
```

#### 样式调整

```scss
.playback-controls {
  display: flex;
  align-items: center;
  gap: 16px;
  padding: 12px 16px;
  background: var(--bg-secondary);
  border-radius: var(--radius-lg);
  user-select: none;

  // 底座模式：去除圆角，融入背景
  &.pedestal {
    border-radius: 0;
    background: #1e1e1e;
    height: 48px;
    padding: 0 20px;
    border: none;

    .controls-progress .time-display {
      font-size: 11px;
    }
  }

  // 紧凑模式保持不变
  &.compact {
    gap: 12px;
    padding: 8px 12px;
  }
}
```

---

### 四、波形图优化 (WaveformTimeline/index.vue)

#### 自定义滚动条设计

**核心改动**：移除浏览器默认滚动条，使用 WaveSurfer 的内置滚动或自定义滚动条组件。

```scss
.waveform-timeline {
  display: flex;
  flex-direction: column;
  height: 100%;
  background: var(--bg-secondary);
  overflow: hidden; // 关键：隐藏外部滚动条
}

.waveform-wrapper {
  flex: 1;
  position: relative;
  min-height: 128px;
  overflow: hidden; // 隐藏默认滚动条

  #waveform {
    height: 100%;

    // 自定义 WaveSurfer 滚动条样式
    :deep(.wavesurfer-scroll) {
      &::-webkit-scrollbar {
        height: 8px;
        background: transparent;
      }

      &::-webkit-scrollbar-track {
        background: #2d333b;
        border-radius: 4px;
        margin: 0 16px;
      }

      &::-webkit-scrollbar-thumb {
        background: #444c56;
        border-radius: 4px;
        transition: background 0.2s;

        &:hover {
          background: #545d68;
        }

        &:active {
          background: #58a6ff;
        }
      }
    }
  }
}

// 如果使用自定义滚动条组件
.custom-scrollbar {
  position: absolute;
  bottom: 24px;
  left: 16px;
  right: 16px;
  height: 8px;
  background: #2d333b;
  border-radius: 4px;
  cursor: pointer;
  z-index: 10;

  .scrollbar-thumb {
    position: absolute;
    height: 100%;
    background: #444c56;
    border-radius: 4px;
    transition: background 0.2s;

    &:hover {
      background: #545d68;
    }

    &.dragging {
      background: #58a6ff;
    }
  }
}
```

#### 配置 WaveSurfer 滚动模式

```javascript
wavesurfer = WaveSurfer.create({
  container: waveformRef.value,
  waveColor: props.waveColor,
  progressColor: props.progressColor,
  cursorColor: props.cursorColor,
  height: props.height,
  normalize: true,
  backend: 'MediaElement',
  plugins: [regionsPlugin, timelinePlugin],
  minPxPerSec: 50,
  scrollParent: true,    // 启用滚动
  fillParent: false,     // 不填充父容器
  autoCenter: true,      // 自动居中播放头
  barWidth: 2,
  barGap: 1,
  barRadius: 2,
  media: document.createElement('audio'),
})
```

---

### 五、字幕列表优化 (SubtitleList/index.vue)

#### 针对 350px 宽度的布局调整

```scss
.subtitle-list {
  display: flex;
  flex-direction: column;
  height: 100%;
  background: var(--bg-primary);
}

// 工具栏精简
.list-toolbar {
  padding: 10px 12px; // 减少内边距

  .search-box {
    max-width: 180px; // 缩小搜索框
  }

  .toolbar-btn {
    width: 30px;
    height: 30px;

    svg { width: 18px; height: 18px; }
  }
}

// 字幕项紧凑布局
.subtitle-item {
  padding: 10px; // 从 12px 减少到 10px
  gap: 10px;

  .item-index {
    width: 28px;  // 从 32px 减少
    height: 28px;
    font-size: 11px;
  }
}

// 时间行优化
.time-row {
  gap: 6px; // 从 8px 减少

  .time-input {
    width: 75px; // 从 100px 减少
    padding: 3px 6px;
    font-size: 11px;
  }

  .time-arrow svg {
    width: 14px;
    height: 14px;
  }

  .duration-tag {
    padding: 2px 6px;
    font-size: 10px;
  }
}

// 文本框优化
.text-input {
  padding: 6px 8px;
  padding-right: 35px;
  font-size: 12px;
  line-height: 1.4;
}

.char-count {
  font-size: 10px;
  right: 6px;
  bottom: 6px;
}

// 操作按钮
.item-actions {
  opacity: 1; // 始终显示，不再依赖 hover

  .action-btn {
    width: 24px;  // 从 28px 减少
    height: 24px;

    svg { width: 14px; height: 14px; }
  }
}
```

---

### 六、任务监控器 (TaskMonitor/index.vue)

#### 组件完整实现

```vue
<template>
  <div class="task-monitor">
    <div class="monitor-header">
      <h3>后台任务</h3>
      <span class="task-count">{{ filteredTasks.length }}</span>
    </div>

    <div class="task-list" v-if="filteredTasks.length > 0">
      <div
        v-for="task in filteredTasks"
        :key="task.id"
        class="task-item"
        :class="{
          'is-current': task.id === currentJobId,
          'is-finished': task.status === 'finished'
        }"
      >
        <!-- 任务信息 -->
        <div class="task-info">
          <div class="task-header">
            <span class="task-name" :title="task.name">{{ task.name }}</span>
            <span class="task-status" :class="task.status">
              {{ getStatusText(task.status) }}
            </span>
          </div>

          <!-- 进度条 -->
          <div v-if="['processing', 'queued'].includes(task.status)" class="task-progress">
            <div class="progress-bar">
              <div class="progress-fill" :style="{ width: task.progress + '%' }"></div>
            </div>
            <span class="progress-text">{{ task.progress }}%</span>
          </div>

          <!-- 完成时间 -->
          <div v-else-if="task.status === 'finished'" class="task-meta">
            <svg class="icon" viewBox="0 0 24 24" fill="currentColor">
              <path d="M9 16.2L4.8 12l-1.4 1.4L9 19 21 7l-1.4-1.4L9 16.2z"/>
            </svg>
            <span>{{ formatTime(task.finishedAt) }}</span>
          </div>

          <!-- 失败原因 -->
          <div v-else-if="task.status === 'failed'" class="task-error">
            <svg class="icon" viewBox="0 0 24 24" fill="currentColor">
              <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm1 15h-2v-2h2v2zm0-4h-2V7h2v6z"/>
            </svg>
            <span>{{ task.error || '转录失败' }}</span>
          </div>
        </div>

        <!-- 操作按钮 -->
        <div class="task-actions">
          <button
            v-if="['processing', 'queued'].includes(task.status)"
            class="action-btn"
            @click="pauseTask(task.id)"
            title="暂停"
          >
            <svg viewBox="0 0 24 24" fill="currentColor">
              <path d="M6 19h4V5H6v14zm8-14v14h4V5h-4z"/>
            </svg>
          </button>

          <button
            v-if="['processing', 'queued', 'paused'].includes(task.status)"
            class="action-btn action-btn--danger"
            @click="cancelTask(task.id)"
            title="取消"
          >
            <svg viewBox="0 0 24 24" fill="currentColor">
              <path d="M19 6.41L17.59 5 12 10.59 6.41 5 5 6.41 10.59 12 5 17.59 6.41 19 12 13.41 17.59 19 19 17.59 13.41 12z"/>
            </svg>
          </button>

          <button
            v-if="task.status === 'finished' && task.id !== currentJobId"
            class="action-btn action-btn--primary"
            @click="openEditor(task.id)"
            title="打开编辑器"
          >
            <svg viewBox="0 0 24 24" fill="currentColor">
              <path d="M3 17.25V21h3.75L17.81 9.94l-3.75-3.75L3 17.25zM20.71 7.04c.39-.39.39-1.02 0-1.41l-2.34-2.34c-.39-.39-1.02-.39-1.41 0l-1.83 1.83 3.75 3.75 1.83-1.83z"/>
            </svg>
          </button>

          <button
            v-if="task.status === 'finished'"
            class="action-btn"
            @click="removeTask(task.id)"
            title="从列表移除"
          >
            <svg viewBox="0 0 24 24" fill="currentColor">
              <path d="M6 19c0 1.1.9 2 2 2h8c1.1 0 2-.9 2-2V7H6v12zM19 4h-3.5l-1-1h-5l-1 1H5v2h14V4z"/>
            </svg>
          </button>
        </div>
      </div>
    </div>

    <!-- 空状态 -->
    <div v-else class="empty-state">
      <svg viewBox="0 0 24 24" fill="currentColor">
        <path d="M19 13h-6v6h-2v-6H5v-2h6V5h2v6h6v2z"/>
      </svg>
      <p>暂无后台任务</p>
    </div>

    <!-- 底部操作 -->
    <div class="monitor-footer" v-if="hasFinishedTasks">
      <button class="clear-btn" @click="clearFinished">
        清除已完成 ({{ finishedCount }})
      </button>
    </div>
  </div>
</template>

<script setup>
import { computed } from 'vue'
import { useRouter } from 'vue-router'
import { useUnifiedTaskStore } from '@/stores/unifiedTaskStore'

const props = defineProps({
  currentJobId: String
})

const router = useRouter()
const taskStore = useUnifiedTaskStore()

// 过滤显示的任务（排除未创建的）
const filteredTasks = computed(() => {
  return taskStore.tasks.filter(t => t.status !== 'created')
})

const hasFinishedTasks = computed(() => {
  return filteredTasks.value.some(t => t.status === 'finished')
})

const finishedCount = computed(() => {
  return filteredTasks.value.filter(t => t.status === 'finished').length
})

function getStatusText(status) {
  const map = {
    'processing': '转录中',
    'queued': '排队中',
    'paused': '已暂停',
    'finished': '已完成',
    'failed': '失败',
    'canceled': '已取消'
  }
  return map[status] || status
}

function pauseTask(id) {
  // 调用后端 API 暂停任务
  console.log('暂停任务:', id)
}

function cancelTask(id) {
  if (confirm('确定要取消这个任务吗？')) {
    taskStore.deleteTask(id)
  }
}

function openEditor(id) {
  router.push(`/editor/${id}`)
}

function removeTask(id) {
  taskStore.deleteTask(id)
}

function clearFinished() {
  const finished = filteredTasks.value.filter(t => t.status === 'finished')
  finished.forEach(t => taskStore.deleteTask(t.id))
}

function formatTime(timestamp) {
  if (!timestamp) return ''
  const date = new Date(timestamp)
  const now = new Date()
  const diff = now - date

  if (diff < 60000) return '刚刚'
  if (diff < 3600000) return `${Math.floor(diff / 60000)} 分钟前`
  if (diff < 86400000) return `${Math.floor(diff / 3600000)} 小时前`
  return date.toLocaleDateString('zh-CN')
}
</script>

<style lang="scss" scoped>
.task-monitor {
  display: flex;
  flex-direction: column;
  max-height: 500px;
  background: #1c2128;
  color: #adbac7;
}

.monitor-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 12px 16px;
  border-bottom: 1px solid #373e47;

  h3 {
    font-size: 14px;
    font-weight: 600;
    margin: 0;
    color: #cdd9e5;
  }

  .task-count {
    padding: 2px 8px;
    background: #373e47;
    border-radius: 10px;
    font-size: 11px;
    font-weight: 600;
    color: #768390;
  }
}

.task-list {
  flex: 1;
  overflow-y: auto;
  padding: 8px;

  &::-webkit-scrollbar {
    width: 8px;
  }
  &::-webkit-scrollbar-track {
    background: transparent;
  }
  &::-webkit-scrollbar-thumb {
    background: #444c56;
    border-radius: 4px;
    &:hover { background: #545d68; }
  }
}

.task-item {
  display: flex;
  gap: 12px;
  padding: 12px;
  margin-bottom: 8px;
  background: #22272e;
  border: 1px solid transparent;
  border-radius: 6px;
  transition: all 0.2s;

  &:hover {
    background: #2d333b;
  }

  &.is-current {
    border-color: #58a6ff;
    background: rgba(88, 166, 255, 0.1);
  }

  &.is-finished {
    opacity: 0.7;
  }
}

.task-info {
  flex: 1;
  min-width: 0;
}

.task-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 8px;
  gap: 8px;

  .task-name {
    flex: 1;
    font-size: 13px;
    font-weight: 500;
    color: #cdd9e5;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .task-status {
    padding: 2px 6px;
    border-radius: 4px;
    font-size: 10px;
    font-weight: 600;
    white-space: nowrap;

    &.processing {
      background: rgba(88, 166, 255, 0.15);
      color: #58a6ff;
    }

    &.queued {
      background: rgba(139, 148, 158, 0.15);
      color: #768390;
    }

    &.finished {
      background: rgba(63, 185, 80, 0.15);
      color: #3fb950;
    }

    &.failed {
      background: rgba(248, 81, 73, 0.15);
      color: #f85149;
    }
  }
}

.task-progress {
  display: flex;
  align-items: center;
  gap: 8px;

  .progress-bar {
    flex: 1;
    height: 4px;
    background: #373e47;
    border-radius: 2px;
    overflow: hidden;

    .progress-fill {
      height: 100%;
      background: #58a6ff;
      transition: width 0.3s ease;
    }
  }

  .progress-text {
    font-size: 11px;
    font-family: monospace;
    color: #768390;
    min-width: 35px;
    text-align: right;
  }
}

.task-meta {
  display: flex;
  align-items: center;
  gap: 6px;
  font-size: 11px;
  color: #768390;

  .icon {
    width: 14px;
    height: 14px;
    color: #3fb950;
  }
}

.task-error {
  display: flex;
  align-items: center;
  gap: 6px;
  font-size: 11px;
  color: #f85149;

  .icon {
    width: 14px;
    height: 14px;
  }
}

.task-actions {
  display: flex;
  flex-direction: column;
  gap: 4px;

  .action-btn {
    width: 28px;
    height: 28px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 4px;
    color: #768390;
    transition: all 0.2s;

    svg {
      width: 16px;
      height: 16px;
    }

    &:hover {
      background: #373e47;
      color: #adbac7;
    }

    &--primary {
      color: #58a6ff;
      &:hover {
        background: rgba(88, 166, 255, 0.15);
      }
    }

    &--danger {
      &:hover {
        background: rgba(248, 81, 73, 0.15);
        color: #f85149;
      }
    }
  }
}

.empty-state {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 48px 24px;
  color: #768390;

  svg {
    width: 48px;
    height: 48px;
    margin-bottom: 12px;
    opacity: 0.5;
  }

  p {
    font-size: 13px;
    margin: 0;
  }
}

.monitor-footer {
  padding: 12px 16px;
  border-top: 1px solid #373e47;

  .clear-btn {
    width: 100%;
    padding: 8px;
    background: #373e47;
    color: #adbac7;
    border-radius: 6px;
    font-size: 13px;
    transition: background 0.2s;

    &:hover {
      background: #444c56;
    }
  }
}
</style>
```

---

## 🎨 配色系统

**保持现有配色方案，仅做微调优化**：

```scss
// CSS 变量 (frontend/src/styles/_variables.scss)
:root {
  // 背景色
  --bg-base: #0d1117;          // 最深背景
  --bg-primary: #161b22;       // 主要背景
  --bg-secondary: #1c2128;     // 次要背景
  --bg-tertiary: #22272e;      // 三级背景
  --bg-elevated: #2d333b;      // 悬浮元素

  // 文字色
  --text-primary: #f0f6fc;     // 主要文字
  --text-normal: #cdd9e5;      // 普通文字
  --text-secondary: #adbac7;   // 次要文字
  --text-muted: #768390;       // 弱化文字

  // 主题色
  --primary: #58a6ff;          // 主题蓝
  --primary-hover: #79c0ff;    // 悬停蓝
  --success: #3fb950;          // 成功绿
  --warning: #d29922;          // 警告黄
  --danger: #f85149;           // 危险红

  // 边框色
  --border-default: #373e47;
  --border-muted: #444c56;

  // 阴影
  --shadow-sm: 0 1px 2px rgba(0, 0, 0, 0.3);
  --shadow-md: 0 4px 8px rgba(0, 0, 0, 0.4);
  --shadow-lg: 0 12px 24px rgba(0, 0, 0, 0.5);

  // 圆角
  --radius-sm: 4px;
  --radius-md: 6px;
  --radius-lg: 8px;
  --radius-full: 999px;

  // 过渡
  --transition-fast: 0.2s ease;
  --transition-normal: 0.3s ease;
}
```

**关键配色调整**：

1. **进度条颜色**：
   - 转录中：`#58a6ff` (主题蓝)
   - 已完成：`#3fb950` (成功绿)
   - 背景轨道：`#444c56`

2. **状态指示点**：
   - 转录中：`#58a6ff`
   - 完成：`#3fb950`
   - 空闲：`#8b949e`

3. **波形图配色**：
   - 波形：`#58a6ff`
   - 进度：`#3fb950`
   - 游标：`#f85149`
   - 字幕区域：`rgba(88, 166, 255, 0.25)`

---

## 📋 测试计划

### 功能测试

1. **顶栏动态切换**
   - 验证转录中显示进度胶囊
   - 验证转录完成后显示队列进度
   - 验证所有任务完成后停在 100%
   - 测试进度胶囊悬浮显示控制按钮
   - 测试任务监控器的打开和关闭

2. **布局响应式**
   - 拖拽分隔条调整侧边栏宽度
   - 验证最小/最大宽度限制 (280px - 600px)
   - 测试窗口缩放时的布局适应
   - 验证 Grid 布局在不同屏幕尺寸下的表现

3. **播放控制**
   - 验证底座模式样式正确应用
   - 测试播放/暂停/快进/快退功能
   - 测试音量和倍速控制
   - 验证进度条拖拽

4. **波形图**
   - 验证自定义滚动条样式
   - 测试缩放功能 (按钮、滚轮)
   - 测试字幕区域的拖拽和调整
   - 验证播放头自动居中

5. **字幕列表**
   - 验证 350px 宽度下的布局
   - 测试字幕编辑功能
   - 测试搜索过滤
   - 验证自动滚动跟随

6. **任务监控器**
   - 验证任务列表显示
   - 测试暂停/取消/打开操作
   - 测试清除已完成任务
   - 验证实时进度更新

7. **底部状态栏**
   - 验证自动保存时间更新
   - 测试设置按钮点击
   - 验证问题数量显示和点击跳转

### 交互测试

1. **键盘快捷键**
   - 空格：播放/暂停
   - 左右箭头：快进/快退
   - Ctrl+Z/Y：撤销/重做
   - F：全屏

2. **鼠标交互**
   - 双击视频：全屏
   - 拖拽分隔条：调整宽度
   - Ctrl+滚轮：波形图缩放
   - 点击字幕：跳转播放

3. **触摸交互** (如适用)
   - 双指缩放
   - 滑动滚动

### 性能测试

1. **大量字幕**
   - 测试 1000+ 条字幕的渲染性能
   - 验证滚动流畅度
   - 检查内存占用

2. **长时间运行**
   - 测试 1 小时以上的编辑会话
   - 验证内存泄漏
   - 检查自动保存稳定性

3. **多任务并发**
   - 同时转录 5+ 个任务
   - 验证进度更新的准确性
   - 测试任务监控器性能

### 兼容性测试

1. **浏览器**
   - Chrome/Edge (主要)
   - Firefox
   - Safari

2. **操作系统**
   - Windows 10/11
   - macOS
   - Linux

3. **屏幕分辨率**
   - 1920x1080 (标准)
   - 2560x1440 (2K)
   - 3840x2160 (4K)
   - 1366x768 (小屏)

### 视觉测试

1. **配色一致性**
   - 验证所有组件使用 CSS 变量
   - 检查暗色主题适配
   - 验证高对比度模式

2. **动画流畅度**
   - 检查进度条更新动画
   - 验证布局切换过渡
   - 测试悬浮效果

3. **细节打磨**
   - 对齐像素完美
   - 间距符合 8px 栅格
   - 字体大小和行高合理

---

## 📦 交付清单

- [ ] `EditorHeader.vue` - 重构的顶栏组件
- [ ] `TaskMonitor/index.vue` - 任务监控器组件
- [ ] `EditorView.vue` - 更新主布局和逻辑
- [ ] `PlaybackControls/index.vue` - 添加底座模式
- [ ] `WaveformTimeline/index.vue` - 优化滚动条样式
- [ ] `SubtitleList/index.vue` - 优化 350px 布局
- [ ] 更新 `_variables.scss` (如需要)
- [ ] 更新 Store 逻辑以支持队列进度计算
- [ ] 添加拖拽调整分隔条的 composable (可选)

---

## 🚀 预期效果

1. **视觉提升**
   - 视频画面占比提升约 30%
   - 界面更加简洁现代
   - 信息层次更清晰

2. **效率提升**
   - 多任务管理更便捷
   - 编辑空间更宽敞
   - 操作路径更流畅

3. **体验优化**
   - 去除手动保存焦虑
   - 实时掌握全局进度
   - 自定义布局适应个人习惯

4. **性能稳定**
   - 优化渲染性能
   - 减少不必要的重绘
   - 提升大量字幕场景的响应速度

---

## 📝 备注

- 本方案保持现有配色系统，确保视觉一致性
- 所有改动向后兼容，不影响现有功能
- 建议分阶段实施，每阶段独立测试
- 用户偏好设置（侧边栏宽度等）需持久化到 localStorage
- 考虑后续添加布局预设切换功能（视频优先/编辑优先）

---

**文档版本**：v1.0
**最后更新**：2025-01-27
